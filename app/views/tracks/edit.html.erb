<% provide(:title, 'Edit Track ' + @track.name) %>

<%= link_to "Back to Tracks", tracks_path(@course), class: "button button-red action-button-top-right" %>
<div class="panel panel-default new-academia-panel">
	<div class="panel-heading">
		<h2 class="panel-title">Update Track: <%= @track.name %></h3>
	</div>
	<div class="panel-body">
		<%= form_for(@track) do |f| %>
			<%= render 'shared/error_messages', object: f.object %>

			<%= f.label :name %>
			<div class="input-group">
				<span class="input-group-addon"><span class="glyphicon glyphicon-plus"></span></span>
				<%= f.text_field :name, class: "form-control Username", placeholder: "Name" %>
			</div>

			<%= f.label :order, 'Order of Track in Course' %>
			<div class="input-group">
				<span class="input-group-addon"><span class="glyphicon glyphicon-list"></span></span>
				<%= f.text_field :order, class: "form-control", placeholder: "Order" %>
			</div>

			<%= f.label :description %>
			<div class="input-group">
				<span class="input-group-addon"><span class="glyphicon glyphicon-align-left"></span></span>
				<%= f.text_area :description, class: "form-control", placeholder: "Description", rows: "6", id: "courseDescription" %>
			</div>

			<%= f.label :permalink %>
			<div class="input-group">
				<span class="input-group-addon"><span class="glyphicon glyphicon-link"></span></span>
				<%= f.text_field :permalink, class: "form-control", placeholder: "Permalink" %>
			</div>

			<%= f.submit "Save changes", class: "btn btn-large btn-primary" %>
		<% end %>
	</div>
</div>
